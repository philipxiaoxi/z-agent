[project]
name = "zspace-agent-backend"
version = "0.1.0"
description = "NAS Agent Backend"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "pydantic-settings>=2.0.0",
]

[dependency-groups]
dev = ["pytest>=8.0.0", "httpx>=0.27.0"]
